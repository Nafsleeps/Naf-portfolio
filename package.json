{
  "name": "naf-portfolio",
  "version": "0.1.0",
  "private": true,
  "description": "Personal site — websites and AI agents for small businesses.",
  "scripts": {
    "build": "tailwindcss -i src/styles.css -o assets/styles.css --minify",
    "dev": "tailwindcss -i src/styles.css -o assets/styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
